A web app that allows searching and filtering of generation 1 Pokémon, and viewing their stats.

Data retrieved using the PokeAPI (https://pokeapi.co/). Requests are cached in memory and local storage.

